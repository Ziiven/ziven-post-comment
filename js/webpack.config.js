// ziven-post-comment webpack config — Flarum 2.0
// Use flarum-webpack-config default (output.library + libraryType = 'assign')
// matching the Flarum 2.0 Extension\Frontend addFile pattern.

const config = require('flarum-webpack-config')();

module.exports = config;
